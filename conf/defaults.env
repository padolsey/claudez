# Global defaults (overridable by ~/.provisionrc or per-sandbox .env)
DOMAIN_BASE=grok.foo
APPS_DIR=/opt/apps
NODE_VERSION=20

# Memory limits - designed for Next.js production builds
# Standard: Good for small-medium projects (up to ~50 pages)
# Large: For heavy builds with 100+ pages or complex dependencies
MEM_RESERVATION=1536m  # Guaranteed memory
MEM_LIMIT=3072m        # Hard limit (3GB - handles most builds)

CPU_LIMIT=1.0
TRAEFIK_NETWORK=edge
# Host path to Anthropic key (read & injected into container .env)
KEY_FILE=/root/ANTHROPIC_KEY.txt
