# Global defaults (overridable by ~/.claudezrc or per-zone .env)
# LOCAL: Use localhost:8080 (default)
# REMOTE: Set DOMAIN_BASE=yourdomain.com in ~/.claudezrc
DOMAIN_BASE=localhost:8080
APPS_DIR=${HOME}/.local/share/claudez/zones
NODE_VERSION=20

# Memory limits - designed for Next.js production builds
# Standard: Good for small-medium projects (up to ~50 pages)
# Large: For heavy builds with 100+ pages or complex dependencies
MEM_RESERVATION=1536m  # Guaranteed memory
MEM_LIMIT=3072m        # Hard limit (3GB - handles most builds)
MEM_LIMIT_LARGE=5120m  # Large profile (5GB)

# Resource limits
CPU_LIMIT=1.0
MAX_SANDBOXES=12       # Conservative: ~36GB total at 3GB each
MAX_SAFE_RESERVED_MB=24576  # 24GB reserved, leaves 6GB for host + spikes

# Networking (auto-detected, but can override)
TRAEFIK_NETWORK=claudez

# Host path to Anthropic key (read & injected into container .env)
# Can be overridden in ~/.claudezrc or via $ANTHROPIC_API_KEY env var
KEY_FILE=~/.config/claudez/anthropic_key

# OpenCode support
OPENROUTER_API_KEY_FILE=~/.config/claudez/openrouter_key
OPENAI_API_KEY_FILE=~/.config/claudez/openai_key

# Gemini CLI support
GEMINI_API_KEY_FILE=~/.config/claudez/gemini_key
